name: Rebuild mkl-service

on:
  workflow_dispatch:

permissions: {}

jobs:
  mkl-service:
    uses: ./.github/workflows/wheels.yml
    permissions:
      contents: write
    with:
      name: mkl-service
      repo: IntelPython/mkl-service
      force-build: true
      # version: 2.8.0
